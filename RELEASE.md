# EhViewerNz 2.0.2.4.7

#### 基于 [EhViewer[xiaojieonly] 2.0.2.4](https://github.com/xiaojieonly/Ehviewer_CN_SXJ/releases/tag/2.0.2.4)

## Fork 更新内容

### 自定义下载分组

- 按标签维度把已下载画廊自动归类：内置原作、作者、AI 生成、动图维度，可自行添加任意标签维度，也可在画廊详情页长按上传者把该上传者加为一个分组。
- 侧边栏新增分组模式（可与“下载标签”模式一键切换）：分组按维度折叠为可展开大类（原作/作者/上传者/自定义标签/其他），默认折叠并记住展开状态，顶部“分组总览”返回总览。
- 支持拖拽调整分组顺序与分组内画廊顺序、多选后“移动到分组”“恢复自动分组”、分组重命名与恢复默认名，列表内以分组头展示各组画廊。
- 分组设置页：启用/禁用维度、拖拽调整优先级、增删标签维度、恢复默认（二次确认）。
- 删除画廊时可选“仅移出分组（不删除文件）”，或从下载列表删除（单分组与分组总览下均提供该选择）。

### 画廊本地元数据

- 下载画廊时把详情页所需元数据保存到该画廊的下载文件夹（`.ehviewer_meta.json`），在线画廊下架或被屏蔽后仍可用本地元数据打开详情页。
- 下载页右上角菜单新增“更新所有画廊本地元数据”，本地导入与在线已下架的画廊自动跳过。

### 排行榜

- “排行榜”改为缩略图卡片显示（与主页一致的网格与列宽设置），封面通过画廊 API 批量获取；时间段标题仍可跳转到对应榜单。

### 画廊更新

- 画廊更新逻辑现在使用其最初版本画廊gid (first_gid)作为版本链依据 (原逻辑通过检查依次检查各个父画廊gid)。
- 在特定画廊浏览页中，为可更新的画廊提供标识。
- 新增“更新已下载画廊的版本信息”按钮，扫描还未添加first_gid信息的已下载画廊并网络请求信息。
- 新增“删除存在已下载新版本的旧版本画廊”按钮，扫描拥有相同first_gid的画廊并保留可阅读的完整最新版本画廊，删除同版本链旧画廊。
- 下载标识添加新状态标识"可更新"。

### 修复

- 修复拖拽排序后“同人杂志/未知/未识别”等固定分组重复显示的问题。
- 修复下载画廊的本地元数据标签未被读取（元数据文件只保存了标签分组、未保存 simpleTags）导致大量画廊落入“未识别”分组的问题。

### 杂项

- 更新至上游仓库 EhViewer 2.0.2.4 新版本。
- 添加画廊长按快速下载的开关设置。
